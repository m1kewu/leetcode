# 11. Container With Most Water
# hint: use two-pointers. Set one pointer to the left and one to the right of the array. Always move the pointer that points to the lower line.
# len >=2 given

def sol(height):
    left = 0
    right = 1
    for i in range(len(height)):



if __name__ == "__main__":
    print(sol([1,8,6,2,5,4,8,3,7])) # 49
    print(sol([1,1])) # 1
